import { RederDirective } from './reder.directive';

describe('RederDirective', () => {
  it('should create an instance', () => {
    const directive = new RederDirective();
    expect(directive).toBeTruthy();
  });
});
